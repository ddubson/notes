import * as React from "react";
import * as ReactDOM from "react-dom";
import {AppRoot} from "./AppRoot";

import "./styles/basic/basic-theme.scss"

ReactDOM.render(
    <AppRoot />,
    document.getElementById("root"),
);
